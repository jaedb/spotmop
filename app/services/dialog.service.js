/**
 * Provide dialog popups
 * These can be sub-forms, confirmations, or just notifications
 **/
 
angular.module('spotmop.services.dialog', [])

.factory("DialogService", ['$rootScope', '$interval', '$timeout', function( $rootScope, $interval, $timeout ){
    
    return {
        create: function()
	};
}]);





